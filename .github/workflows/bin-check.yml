name: bin check
on:
  push:
    branches:
      - master

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build image
        run: |
          bash docker_build.sh
